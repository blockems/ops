module.exports = {
// uncomment for dev environment

    //Jira server name
        jiraServer: 'jira.eceengine.com',
        jirAPI: 'rest/api/latest',
        bffServer: 'localhost',
        bffPort: 1337,
    //Jira server log in details

    //Base directory name (./database)
        datadir: './database',
        server: 'localhost',
        port: 3001,
        debuglevel: 2,
        environment: 'dev'

// uncomment for tes environemnt in aws

    //Jira server name

    //Jira server log in details

    //Base directory name (./database)

}